<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  <meta http-equiv="x-ua-compatible" content="ie=edge" />
  <link rel="shortcut icon" href="/images/votewiseLogo.png" type="image/x-icon" />
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.11.2/css/all.css" />
  <!-- Google Fonts Roboto -->
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" />
  <!-- Bootstrap CSS -->
  <link href="/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Custom styles -->
  <link rel="stylesheet" href="/css/admin.css" />
  <link rel="stylesheet" href="/css/style.css" />
  <!-- Additional Libraries -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.4/Chart.min.js" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

  <title>Votewise | <%= user.first_name %> <%= user.middle_name %> <%= user.last_name %></title>

  <style>
    body {
      font-family: 'Roboto', sans-serif;
      background-color: #f8f9fa;
      color: #333;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 700px;
      margin: 6rem auto 3rem;
      background: #fff;
      border-radius: 15px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
      padding: 30px 40px;
    }

    h1 {
      font-weight: 700;
      color: #0d6efd;
      text-align: center;
      margin-bottom: 2rem;
      font-size: 2.5rem;
    }

    .profile-img-container {
      text-align: center;
      margin-bottom: 1.5rem;
    }

    .profile-img-container img {
      width: 160px;
      height: 160px;
      object-fit: cover;
      border-radius: 50%;
      border: 5px solid #0d6efd;
      box-shadow: 0 0 15px rgba(13, 110, 253, 0.3);
    }

    .profile-name {
      text-align: center;
      margin-bottom: 1rem;
    }

    .profile-name h4 {
      font-size: 1.8rem;
      margin: 0;
      color: #212529;
      font-weight: 600;
    }

    .profile-name h6 {
      font-size: 1.1rem;
      color: #6c757d;
      margin-top: 0.2rem;
      font-weight: 400;
    }

    .status-container {
      text-align: center;
      margin: 2rem 0;
    }

    .status-container span {
      font-size: 1rem;
      font-weight: 600;
      margin: 0 0.5rem;
      vertical-align: middle;
    }

    .badge {
      font-size: 1rem;
      padding: 0.5em 1em;
      border-radius: 20px;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .info {
      background-color: #f1f3f5;
      border-radius: 12px;
      padding: 25px 30px;
      box-shadow: inset 0 0 5px #ddd;
    }

    .info p {
      font-size: 1.3rem;
      font-weight: 700;
      color: #495057;
      margin-bottom: 1rem;
      border-bottom: 2px solid #0d6efd;
      padding-bottom: 8px;
      letter-spacing: 0.03em;
    }

    .info span {
      display: block;
      font-size: 1.05rem;
      color: #343a40;
      margin: 10px 0;
      font-weight: 500;
    }

    @media (max-width: 768px) {
      .container {
        margin: 3rem 1rem 2rem;
        padding: 20px 25px;
      }

      h1 {
        font-size: 2rem;
      }

      .profile-img-container img {
        width: 130px;
        height: 130px;
      }

      .info p {
        font-size: 1.1rem;
      }

      .info span {
        font-size: 1rem;
      }
    }
  </style>
</head>

<body>
  <header>
    <%- include('./layout/navbar') -%>
  </header>

  <main class="container" role="main" aria-label="Voter Profile">
    <h1>Voter Profile</h1>

    <div class="profile-img-container">
      <img src="data:image/png;base64,<%= user.profile_picture %>" alt="Profile picture of <%= user.first_name %>" />
    </div>

    <section class="profile-name" aria-label="User Full Name and Username">
      <h4><%= user.first_name %> <%= user.middle_name %> <%= user.last_name %></h4>
      <h6>@<%= user.username %></h6>
    </section>

    <section class="status-container" aria-label="User Role and Vote Status">
      <span>Role:</span>
      <span id="role-badge" class="badge bg-primary"><%= user.role %></span>
      <span>Status:</span>
      <span id="vote-status-badge" class="badge bg-<%= voteStatus === 'Voted' ? 'success' : 'danger' %>"><%= voteStatus %></span>
    </section>

    <section class="info" aria-label="Personal Information">
      <p>Personal Info</p>
      <span><strong>First Name:</strong> <%= user.first_name %></span>
      <span><strong>Middle Name:</strong> <%= user.middle_name || 'N/A' %></span>
      <span><strong>Last Name:</strong> <%= user.last_name %></span>
      <span><strong>Date of Birth:</strong> <%= formattedDOB %></span>
    </section>
  </main>

  <%- include('layout/footer') %>
</body>

</html>
